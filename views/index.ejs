<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .header {
            background-color: #343a40;
            color: #ffffff;
            padding: 2rem 0;
            margin-bottom: 2rem;
            text-align: center;
        }
        .post-card {
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 2rem;
            transition: transform 0.3s ease;
        }
        .post-card:hover {
            transform: translateY(-5px);
        }
        .post-header {
            background-color: #007bff;
            color: #ffffff;
            padding: 1rem;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
        }
        .post-content {
            padding: 1.5rem;
        }
        .post-footer {
            padding: 1rem;
            background-color: #f8f9fa;
            border-bottom-left-radius: 8px;
            border-bottom-right-radius: 8px;
        }
        .btn-group {
            display: flex;
            justify-content: space-between;
        }
        .new-post-btn {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            z-index: 1000;
        }
    </style>
</head>
<body>
    <header class="header">
        <h1><i class="fas fa-blog"></i>  Blog</h1>
    </header>

    <div class="container">
        <div class="row">
            <% for(data of datas) { %>
                <div class="col-md-6 col-lg-4">
                    <div class="post-card">
                        <div class="post-header">
                            <h2><%= data.username %></h2>
                        </div>
                        <div class="post-content">
                            <p><%= data.content.length > 150 ? data.content.substring(0, 150) + '...' : data.content %></p>
                        </div>
                        <div class="post-footer">
                            <div class="btn-group">
                                <a href="/posts/<%= data.id %>" class="btn btn-primary btn-sm">
                                    <i class="fas fa-eye"></i> View
                                </a>
                              
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    <a href="/posts/new" class="btn btn-success btn-lg new-post-btn">
        <i class="fas fa-plus"></i> New Blog Post
    </a>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>